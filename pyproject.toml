[project]
name = "proxy-python"
version = "0.1.0"
description = "Simple Python Proxy Server"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["proxy"]
